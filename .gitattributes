# 기본: 텍스트 파일은 체크아웃 시 OS 기본 줄바꿈, 저장소에는 LF
* text=auto

# 컨테이너/리눅스에서 실행되는 파일은 LF 강제
# (Windows 에서 CRLF 로 저장되면 'bad interpreter: /bin/bash^M' 에러 발생)
*.sh text eol=lf
*.py text eol=lf
Dockerfile text eol=lf
Makefile text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

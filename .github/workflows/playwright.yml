# Playwright 테스트 CI — push/PR 마다 전체 테스트 실행 후 HTML 리포트를 아티팩트로 업로드
name: "playwright - saucedemo"
# Actions 탭 목록에 표시될 런 제목.
#   schedule/수동 실행은 연관 커밋이 없어 기본값이 워크플로 이름이라 서로 구분되지 않는다.
#   그 두 경우만 덮어쓰고, 나머지는 빈 문자열을 주어 기본 동작(push=커밋 메시지, PR=PR 제목)에 맡긴다.
#   ("run-name 이 생략되거나 공백뿐이면 이벤트별 기본 정보로 설정된다" — GitHub 워크플로 문법 문서)
run-name: "${{ github.event_name == 'schedule' && '[Daily] Regression Test' || github.event_name == 'workflow_dispatch' && '수동 실행' || '' }}"

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  # Actions 탭에서 수동 실행 가능
  workflow_dispatch:
  # 데일리 레그레션(런 제목: [Daily] Regression Test) — 코드 변경이 없어도 매일 1회 실행해 대상 사이트(saucedemo) 변화와
  # 플레이키 테스트를 감지한다. cron 은 UTC 기준이며 기본 브랜치에서만 동작한다.
  #   '5 18 * * *' = UTC 18:05 = KST 03:05 (UTC 날짜 +1일이 KST 날짜)
  #   정시(:00)는 GitHub 부하가 커 지연되므로 일부러 5분으로 둔다.
  #   새벽으로 둔 이유 — 스케줄은 "이 시각 이후 여유될 때" 발동한다.
  #   실측: 평소 +19분, GitHub Actions 장애 시 +203분(2026-08-27).
  #   03:05 면 3시간 지연돼도 06:30 이전에 끝나 출근 전 결과가 보장된다.
  schedule:
    - cron: '5 18 * * *'

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: pyproject.toml

      - name: 의존성 설치
        run: pip install -e .

      - name: Playwright 브라우저 설치
        run: python -m playwright install --with-deps chromium

      # 값에 콜론(:)이 있으면 YAML 이 "키: 값" 구분자로 오인하므로 반드시 따옴표로 감싼다
      - name: "테스트 실행 (CI: 실패 시 2회 재시도)"
        run: python -m pytest --reruns 2

      - name: 리포트 업로드 (실패 시에도 항상)
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: |
            reports/
            test-results/
          retention-days: 30
